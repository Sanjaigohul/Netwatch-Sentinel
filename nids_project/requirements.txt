# ============================================================
#  NIDS – Python Dependencies
# ============================================================

# ── Web Framework ─────────────────────────────────────────────
flask>=3.0.0
flask-socketio>=5.3.6
flask-cors>=4.0.0
python-socketio>=5.10.0
python-engineio>=4.8.0
eventlet>=0.35.1
gevent>=23.9.1
gevent-websocket>=0.10.1
werkzeug>=3.0.1

# ── Machine Learning ──────────────────────────────────────────
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.2.0
joblib>=1.3.2

# ── Deep Learning (optional – gracefully degrades) ────────────
# tensorflow>=2.15.0        # uncomment if GPU/CPU TF available
# keras>=2.15.0

# ── Database ──────────────────────────────────────────────────
mysql-connector-python>=8.3.0

# ── Packet Capture ────────────────────────────────────────────
scapy>=2.5.0               # requires root / raw-socket privileges

# ── Utilities ─────────────────────────────────────────────────
python-dotenv>=1.0.0
requests>=2.31.0

# ── Optional Boosters ─────────────────────────────────────────
# xgboost>=2.0.3            # uncomment for XGBoost classifier
# pyshark>=0.6              # alternative packet capture
